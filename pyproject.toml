[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lachesis"
version = "0.1.0"
description = "Isochrone fitting with Bayesian Model Averaging"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "scipy",
    "h5py",
    "requests",
    "astropy",
    "astroquery",
    "regions",
    "pandas",
    "dynesty>=3.0.0",
    "numba",
    "arviz",
    "h5netcdf",
    "termcolor",
    "dustmaps>=1.0.14",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.hatch.build.targets.wheel]
packages = ["lachesis"]

[tool.hatch.build.targets.wheel.force-include]
"lachesis/Datafiles" = "lachesis/Datafiles"

[tool.pytest.ini_options]
testpaths = ["tests"]
